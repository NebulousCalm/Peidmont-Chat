const openNav = () => {
  document.getElementById("mySidenav").style.width = "250px";
}

const closeNav = () => {
  document.getElementById("mySidenav").style.width = "0";
}

/* const eval = () => {
  const x = document.getElementById('name').value;
  if (x !== ""){
    var y = 0; // This is just a dummy and will not actually do anything just so we can have the else statement lol
  } else {
    alert('Please Enter Something Into The Chat Before Submitting!');
  }
} */